# Blog Writer App 기술 명세서

## 프로젝트 개요
- 프로젝트명: Blog Writer App
- 목적: 네이버 블로그 상위 노출 벤치마크 기반의 글 구조 템플릿을 빠르게 생성해 사용자가 네이버 블로그에 붙여넣을 수 있도록 지원하는 경량 웹 앱 제공
- 호스팅: GitHub Pages (`docs/` 폴더)
- 대상 사용자: 블로거, 콘텐츠 마케터, 개인 창작자

## 주요 기능
1. 글 기본 정보 입력
   - 제목, 부제목, 요약, 인트로
   - 키워드(쉼표 구분), 태그(쉼표 구분), CTA
2. 섹션 편집
   - 섹션 추가/삭제
   - 각 섹션: 소제목, 리드 문장, 핵심 포인트(리스트), Takeaway
3. 이미지 관리
   - 이미지 URL 목록 입력(쉼표 구분)
   - 권장 이미지 수 안내(기본 4)
4. 출력
   - 실시간 미리보기(HTML)
   - Markdown 변환 및 복사
5. 템플릿과 예시
   - 섹션 템플릿 및 예시 불러오기
6. 배포
   - 정적 사이트로 GitHub Pages 배포

## 사용자 스토리 (우선순위)
- US-1: 사용자가 제목·요약 입력 후 `생성` 버튼으로 미리보기/Markdown을 얻는다.
- US-2: 사용자가 섹션을 추가/제거하고 각 섹션의 포인트를 편집할 수 있다.
- US-3: 사용자가 생성된 Markdown을 복사해 네이버 블로그 에디터에 붙여넣는다.
- US-4: 사용자가 예시 로드로 템플릿을 즉시 적용한다.

## 아키텍처 및 기술 스택
- 프론트엔드: HTML, CSS, Vanilla JavaScript (현재 구현)
- 호스팅: GitHub Pages (Branch: `master`, Folder: `/docs`)
- 버전관리: Git + GitHub
- 선택적 확장:
  - 빌드 도구: Vite 또는 Parcel (필요 시)
  - CI: GitHub Actions (린트 및 자동 배포)

## 파일/데이터 구조
- 루트/
  - `docs/` — 배포 파일 (`index.html`, `style.css`, `app.js`)
  - `blog_writer_app/` — 개발용 복사본
  - `naver_blog_structure.py` — 파이썬 템플릿 도우미
  - `TECHNICAL_SPEC.md` — (이 파일)
  - `README.md`
  - `.nojekyll`

## UI/UX 흐름
1. 사용자가 기본 정보를 입력
2. 섹션 편집 영역에서 섹션을 추가/편집
3. `생성` 클릭 시 우측/하단에 미리보기 및 Markdown 출력
4. 원하는 내용 수정 후 Markdown 복사 → 네이버 블로그에 붙여넣기

## 데이터 및 동작 규칙
- 섹션 `bullets`는 쉼표로 구분된 텍스트 입력을 내부 배열로 변환
- Markdown 규칙: `### 소제목` → 리드 문장 → `- 포인트` 리스트 → **Takeaway**
- 이미지 업로드 기능은 포함하지 않음(사용자가 직접 업로드 후 URL 입력)

## 배포 및 CI/CD
- 배포 방식: GitHub Pages (docs 폴더)
- 권장 CI:
  - Push → GitHub Actions: lint 체크 → (선택) 빌드 → 자동 Pages 갱신

## 테스트 전략
- 수동 테스트: 데스크탑/모바일 브라우저에서 UI 동작 확인
- 자동 테스트(옵션): Playwright로 주요 사용자 플로우 시나리오 테스트

## 운영 및 모니터링
- 버그/이슈 트래킹: GitHub Issues 사용
- 사용자 통계(옵션): Google Analytics 또는 Plausible 연동

## 향후 확장 아이디어
- 네이버 블로그 API를 이용한 자동 게시(네이버 API 권한 필요)
- 이미지 업로드 기능(간단한 S3 대안 또는 서드파티 사용)
- 사용자 계정과 저장된 템플릿 기능
- 마크다운 파일로 내보내기, 또는 CMS 연동

---
작성: 자동 생성된 기술 명세서. 필요 시 항목별 세부 요구사항(엔드포인트, 컴포넌트 설계, 테스트 케이스)을 추가 작성합니다.
